import * as React from 'react';
import { Layout } from "../components";

export const Catalog: React.FC = () => {
    return(
        <Layout title={"Welcome to Otus Online Store"}>Catalog</Layout>
    )
}
